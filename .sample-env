PGUSER = // Postgres Username
PGPASSWORD = // Postgres Password
PGDATABASE = // Postgres Database
PGHOST = // Postgres IP
PGPORT = // Postgres Port
MQTT_HOST = // MQTT IP
MQTT_PORT = // MQTT Port
MQTT_CLIENTNAME = // MQTT client name for iot-system, can be anything
SFTP_HOST = // SFTP IP
SFTP_USERNAME = // SFTP Username
SFTP_PASSWORD = // SFTP Password
SFTP_UPLOAD_PATH = // Upload path of files on SFTP server (ex: C:\FileServer\Uploads)
HASH_SECRET = // Secret string for hashing the passwords
