html
    head
        link(rel="stylesheet", href="/static/stylesheets/style.css")
        title Topic Configuration Screen
    body
        h1 Topic configuration screen

        p Topic List
        ul
            for topic in topicList
                li= content.content_url
            else
                li There are no topics in the database.
        
        if errorMessage
            h2= errorMessage